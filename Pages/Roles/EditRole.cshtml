@page
@model FDFLodsedler.Pages.Roles.EditRoleModel
@{
}
<h1>Ret Rolle </h1>
<form method="post" class="mt-3">
    <div class="form-group">
        <label asp-for="EditRole.Id"></label>
        <input asp-for="EditRole.Id" disabled class="form-control" />
        <span asp-validation-for="EditRole.Id" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="EditRole.RoleName"></label>
        <input asp-for="EditRole.RoleName" value="@Model.EditRole.RoleName" class="form-control" />
        <span asp-validation-for="EditRole.RoleName" class="text-danger"></span>
    </div>
    <div>
        <input type="hidden" asp-for="EditRole.Id" class="form-control" />
        <input type="submit" value="Edit Role" class="btn btn-primary" />
        <button type="submit" class="btn btn-primary">Opdater</button>
        <a asp-page="/Roles/GetRoles" class="btn btn-primary">Annuller</a>
    </div>
    @*<div class="form-group row">
    <div class="col-sm-10">

    <button type="submit" class="btn btn-primary"> Update</button>
    <a asp-page="/Roles/GetRoles" class="btn btn-primary"> Cancel</a>

    </div>*@

@*     </div>
 *@
    <div class="card">
        <div class="card-header">
            <h3>Users in this Role</h3>
        </div>
        <div class="card-body">
            @if (Model.EditRole != null)
            {
                foreach (var user in Model.EditRole.Users)
                {
                    <h5 class="card-title">@user</h5>
                }
                {
                }
            }
        </div>
        <div class="card-footer">
            <p>
                <a asp-page="/Roles/EdithUsersInRole" asp-route-sid="EditRole.Id" class=" btn btn-primary" style="width:auto">Add or User</a>
            </p>
        </div>
    </div>
</form>
