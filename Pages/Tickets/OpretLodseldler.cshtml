@page
@model FDFLodsedler.Pages.Tickets.OpretLodseldlerModel
@{
}
<h1>Udlever Lodsedler</h1>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="  ModelOnly  " class="text-danger">
            </div>
          @* <div class="form-group">
                <label asp-for="@Model.Lodseldler.BørneId" class="control-label"></label>
                <input asp-for="@Model.Lodseldler.BørneId" class="form-control" />
                <span asp-validation-for="@Model.Lodseldler.BørneId" class="text-danger"></span>
            </div>*@
            <div>
            <label asp-for="@Model.Lodseldler.BørneId" class="control-label">Barn Navn</label>
                <select class=" form-control" asp-for="@Model.Lodseldler.BørneId">
            @if (Model.Børns != null)
            {
            @foreach (var item in Model.Børns)
              {
               <option value="@item.Børn_Id">@item.Navn</option>
              }
               }
                </select>
               @* <input asp-for="@Model.Lodseldler.BørneId" class="form-control" />*@
                <span asp-validation-for="@Model.Lodseldler.BørneId" class="text-danger"></span>
            </div>         
            <div class="form-group">
                <label asp-for="@Model.Lodseldler.AntalUdleveret" class="control-label"></label>
                <input asp-for="@Model.Lodseldler.AntalUdleveret" class="form-control" />
                <span asp-validation-for="@Model.Lodseldler.AntalUdleveret" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
